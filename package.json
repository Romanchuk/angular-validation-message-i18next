{
    "name": "angular-validation-message-i18next",
    "version": "0.1.0",
    "author": {
        "name": "Sergey Romanchuk"
    },
    "homepage": "https://github.com/Romanchuk/angular-validation-message-i18next#readme",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/Romanchuk/angular-validation-message-i18next.git"
    },
    "license": "MIT",
    "description": "angular directive that inserts validation message right after your validating control",
    "engines": {
        "node": "4.4.4"
    },
    "scripts": {
        "karma": "npx karma",
        "rimraf": "npx rimraf",
        "ngc": "npx ngc",
        "cpy": "npx cpy",
        "rollup": "rollup",
        "prebuild": "npm run clean",
        "compile": "npm run compile:es2015 && npm run compile:es5",
        "compile:es2015": "npm run ngc -- -p src/tsconfig-es2015.json && npm run rollup -- build/angular-validation-message-i18next.js -o dist/angular-validation-message-i18next.js -f es",
        "compile:es5": "npm run ngc -- -p src/tsconfig-es5.json && npm run rollup -- build/angular-validation-message-i18next.js -o dist/angular-validation-message-i18next.es5.js -f es",
        "copy:assets": "npm run cpy -- \"**/*.*\" \"!**/*.js\" \"../dist\" --parents --cwd=build && npm run cpy -- src/package.json dist && npm run cpy -- README.md dist && npm run cpy -- CHANGELOG.md dist",
        "conventional-changelog": "conventional-changelog",
        "changelog": "npm run conventional-changelog -- -p angular -i CHANGELOG.md -s",
        "clean": "rimraf dist/",
        "clean:tests": "npm run rimraf -- coverage && npm run rimraf -- \"tests/**/*.+(js|js.map|d.ts|metadata.json)\" ",
        "clean:compile": "npm run rimraf -- dist build",
        "lint": "npm run lint-ts && npm run lint-js && npm run lint-css",
        "lint-css": "stylelint './src/**/*.css'",
        "lint-ts": "tslint './src/**/*.ts'",
        "lint-js": "eslint '**/*.js' --ignore-path .gitignore",
        "preversion": "npm run clean:compile && npm run compile && npm run copy:assets",
        "version": "npm run changelog && git add CHANGELOG.md",
        "postversion": "git push && git push --tags && npx conventional-github-releaser -p angular",
        "test": "npm run clean:tests && karma start karma.conf.js",
        "test:watch": "npm run clean:tests && karma start karma.conf.debug.js"
    },
    "dependencies": {
        "@angular/core": ">=4.0.0 <6.0.0",
        "@angular/forms": ">=4.0.0 <6.0.0",
        "angular-i18next": "^3.1.0",
        "angular-validation-message": "^0.1.0"
    },
    "devDependencies": {
        "@angular/compiler": ">=4.0.0 <6.0.0",
        "@angular/compiler-cli": ">=4.0.0 <6.0.0",
        "@types/chai": "^4.0.4",
        "@types/i18next": "^8.4.2",
        "@types/jasmine": "^2.6.2",
        "@types/sinon": "^2.3.7",
        "chai": "^4.1.2",
        "conventional-changelog-cli": "^1.3.4",
        "conventional-github-releaser": "^2.0.0",
        "coveralls": "^3.0.0",
        "codelyzer": "^4.0.1",
        "rimraf": "^2.6.2",
        "rollup": "^0.50.0",
        "sinon": "^4.1.1",
        "jasmine": "^2.8.0",
        "jasmine-core": "^2.8.0",
        "karma": "^1.7.1",
        "karma-chrome-launcher": "^2.2.0",
        "karma-coverage": "^1.1.1",
        "karma-jasmine": "^1.1.0",
        "karma-sourcemap-loader": "^0.3.7",
        "karma-spec-reporter": "^0.0.31",
        "tslint": "^5.8.0",
        "typedoc": "^0.9.0",
        "typescript": "2.4.2",
        "zone.js": "^0.8.18",
        "cpy-cli": "^1.0.1"
    }
}